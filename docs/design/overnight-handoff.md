# Single-WorkOrder offline handoff | WorkOrder #45

The [activation 1.0](https://github.com/pym96/Pan-agent/issues/45#issuecomment-5584581317) authorizes a separate [private development utility](../../devtools/overnight/README.md) from accepted base `eda8bd58e95ff52af95fe2e021ebf6c7778ba74f`. Contract body with terminal LF SHA-256: `16201f3754e32226b7fdedd1ff26b09f17aa0b0ba6e72d948bb140d1dee7d5d0`. This candidate does not change Product runtime, TUI, archives or accepted implementation facts.

The coordinator reduces manual message transport by executing a preauthorized finite route. A fixed fake Builder runs in its own worktree/session and pushes a new candidate to a disposable remote. Its successful structured result is checked against manifest, evidence and remote identity before publication to an exclusive-key fake tracker. A separate fake Regulator receives frozen task/criteria and public evidence identity, with a fresh clean detached worktree at that exact remote candidate. A rejected existing criterion can reserve one repair; a new candidate always gets a new review. Accepted stops pending Master and never changes main.

The persistent boundary precedes launch and publication. On recovery, the process receipt, stable PID start/command identity, completed child status, raw fixture result and immutable tracker record are reconciled. Intent without confirmed effects is not replayed. Terminal status is observational. Corrupt ledgers retain original bytes; status reports needs_reconciliation. Live mode fails before workspace, process, credentials or network use. An unrelated or reused PID is not signaled. Locks only govern cooperating coordinators sharing the state registry.

Time and authority are checked separately from process exit. Original wall expiry persists; monotonic elapsed time applies while alive. Only typed existing-ID criterion_failed routes back. Missing Human evidence, quota, ScopeChallenge, malformed output and ambiguity stop with distinct reasons. Ordinary descendants are cancelled as an owned group, with TERM/KILL and explicit bounded cleanup evidence. No generic exactly-once, universal secret detection, arbitrary-code sandbox or real coding-Agent interoperability is claimed.

The [seven-criterion obligation map](workorder-45-obligations.json) separates standard evidence from the high-risk A-IDENTITY, A-CANCEL and A-CONTAINMENT review. [Candidate evidence](../evidence/overnight-handoff-offline-candidate.md) and a full-SHA-bound Handoff are Builder claims only. Independent Regulator and different-family or explicit Human review of those three named outputs are still required.

Current Pan/Reference/Python checks run unchanged. Only new development-tool files and explicitly authorized documentation indexes differ from base. The outer host check uses its unchanged validator and the contract's explicit disposable-copy exclusions, reporting original-host BLOCK separately. Root host navigation and actual resume/fact files are not edited by this candidate.

The F1–F3 repair retains Criteria-Version 1.0 and unifies initial and recovered result validation. Recorded tracker data is compared with its saved digest and original role result, then checked against role/session/candidate and evidence/process identities before outcome/blockers can select a transition. Recovery errors use a fixed category and cannot reflect untrusted fields. [Real-process regression](../../devtools/overnight/test/recovery-integrity.test.ts) covers this boundary.

## F4: historical trust failure must not bypass lifecycle cleanup

Under the [Master F4 repair dispatch](https://github.com/pym96/Pan-agent/issues/45#issuecomment-5594424647), Criteria-Version stays 1.0. Recorded-result validation still precedes route decisions. Its failure now reaches a bounded cleanup path on explicit recovery when a current unrecorded attempt exists. Ownership comes from that current attempt and matching receipt/PID/start identity, independently of any historical result. Stop and original role/total deadlines select the lifecycle reason; invalid history also stops owned work when no explicit stop is pending. Cleanup remains unconfirmed if the current receipt conflicts or its PID identity is reused.

The final integrity reason is retained even after successful cleanup. `history-reconciliation.json` separately identifies the attempted cleanup outcome, while original history stays untouched and attempt stop/lifecycle files hold actual observations. No repair is reserved and no next role is launched. The [F4 regression](../../devtools/overnight/test/recovery-stop.test.ts) retains `R-F4.json` alongside the original seven A-* outputs and F1–F3 regression. This is a repair of C-AUTO-05, not a new runtime feature or acceptance.
