# #71 Builder evidence — Criteria1.1

Builder candidate; independent Regulator and Human reviews pending. Base `2291fc7856cb5c752afbf804d2920f1bd900eeee`, branch `workorder/71-candidate`. Handoff binds the full pushed SHA. Product source and all protected tracked files remain unchanged.

Evidence root: `/Volumes/WD_BLACK/pan-agent/wo71-benchmark-env-20260921/`; active APFS staging `/private/tmp/wo71-work`. [Runbook](../../scripts/benchmark-env/README.md), [design](../design/benchmark-env-tracer.md). Final raw manifest/check counts and archive hashes are bound in the Handoff. These are environment/evaluator fixtures, not learned benchmark results or accepted facts.

## Original evidence and revision lineage

SC-BENV-71-01 and its Master Criteria1.1 resolution are retained on [#71](https://github.com/pym96/Pan-agent/issues/71). Original create counterexample SHA256 `56da43cee8a923043a893fa11538ebda18834be5fbc9405ff0b6b667e36d128c`; original postflight `9a09e8c3d4cecb1f022ea3539c836c9d1fb19a362f3397f6809be9b27d6ae576`. Additional cleanup counterexample records the original subprocess bypass. No unsafe evaluator was started.

The original658.7880845829968 seconds of acquisition/venv receipts and11.626 seconds of startup remain in the cumulative ledger; no baseline reset. Original baseline Docker.raw allocation34993225728 bytes and free102592065536 bytes remain recorded. The earlier startup/preflight samples are also retained. Individual step receipts, not a mutable aggregate alone, reconstruct consumption. The exFAT `._` JSON-discovery/aggregate failures are retained in `bookkeeping-failure.txt`; successful acquisitions were not repeated to replace bookkeeping failures.

Retained development failures include internal-network port absence (v1); upstream0.1s readiness requests incompatible with relay latency (v2); an incorrect controller assertion expecting DA `func` to be a scalar rather than the exact singleton list (v6); and an over-strict ownership comparison against inherited public image labels on cancellation (v7). The repair checks required ownership keys and records stopped state. A v8 assertion that sysfs listed only `lo` failed on Linux tunnel pseudo-interfaces; current probes record interfaces, require no IPv4 routes and test controlled host connections. No contract capability/resource ceiling was relaxed. Original and repaired manifests/receipts are separate; pre-final trace/log archives preserve overwritten fixture outputs.

## Builder observations

The same installed Pan package runs both scripted task traces: ordinary output, real writes, exit7 and explicitly truncated5000-character output; separate timeout/cancellation fixtures record owned-container termination. Package SHA256 remains `98b190d97cce81a53706c0766dd94626c6a406f4b291764bfb919e2c70da345f`; normalized installed runtime is checked against `6d9b75058698a2db046af1ea0462313ae4fce0c9b7118a97708d239df8c57f6b`. Fixture exchange counts are not real model calls.

Official SWE gold yielded resolved=true; incorrect nonempty patch applied successfully and yielded resolved=false, infra_failure=false, with five required FAIL_TO_PASS failures. DA gold yielded1.0; the same-schema altered categorical result yielded0.5. Gold is evaluator-only. The Pan DA export is an explicitly synthetic transcript projection with field provenance, not DA-Agent output or task success. Missing/corrupt-output classification tests are distinct from low scores.

Finite probes cover controlled host file/service, synthetic credential environment, evaluator-gold path, prior-task marker, unauthorized/cache-key clients, safe export and absolute/traversal/outward-symlink refusal. A real never-started foreign-owner collision fixture is retained unchanged through upstream409 retry and facade removal refusal, then removed by its original controller using its recorded ID/labels. Unknown methods/options and raw object forwarding are tested separately. Postflight inventories and all raw facade/config receipts are retained.

Real model/provider/account/quota calls and new fees remain zero. #70 activation is not used. No main push, benchmark campaign, resume/VPF/Wiki changes or self-acceptance. C-BENV-06 awaits independent Regulator and Human H-BENV-BOUNDARY/H-BENV-ORACLE judgments bound to the candidate, including the facade/relay and finite limits.
