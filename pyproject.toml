[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "workspace-agent-harness"
version = "0.0.1"
description = "A bounded, traceable harness for local workspace agents"
requires-python = ">=3.11"
dependencies = []
