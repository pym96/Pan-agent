# Tools

Tools implement the shared AgentTool interface using Node APIs. The existing trusted-local authority boundary is unchanged.

- [`pan-trusted-local-tools.ts`](pan-trusted-local-tools.ts): existing read/write/edit/bash implementations and factory.
