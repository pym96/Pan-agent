# Pan Faux

Faux remains the reusable deterministic Adapter used by offline Product tests and the frozen installed tracer.

- [`faux-model-adapter.ts`](faux-model-adapter.ts): existing canonical scripted ModelAdapter.

#44 optional progress(index, signal) supplies an AsyncIterable of provisional public fragments. The same exchange still settles to its original authoritative script entry. Abort closes the source without requiring its next item.
