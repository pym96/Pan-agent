# TUI

TUI consumes Session/observation and archive interfaces. It owns no model/tool loop or concrete Provider/Tool. Presentation changes belong to another WorkOrder.

- [`tui.ts`](tui.ts): existing TUI, observation projection and archive replay.
