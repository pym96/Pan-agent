# Shared protocol

This Module has no dependency on concrete implementations.

- [`canonical-protocol.ts`](canonical-protocol.ts): canonical messages, results, usage and validation.
- [`agent-tool.ts`](agent-tool.ts): Tool definition, argument validation and execution contract.
- [`model-adapter-contract.ts`](model-adapter-contract.ts): canonical model exchange contract.
